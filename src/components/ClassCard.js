import React from 'react'

export default function ClassCard(props) {
    const {data: {
        name, 
        type, 
        start, 
        duration, 
        intensity, 
        location, 
        registered,
        max
    }} = props;
    return (
        <div>
            
        </div>
    )
}
